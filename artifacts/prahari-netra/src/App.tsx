import { type ReactNode, useState } from 'react';
import { Link, Route, Switch, useLocation, useParams } from 'wouter';
import {
  Activity, AlertTriangle, ArrowLeft, ArrowRight, BadgeCheck, Bell, Camera,
  Check, CheckCircle2, CircleDot, Crosshair, Database, Download, Eye, FileCheck2,
  FileKey2, Fingerprint, Flag, Gauge, Hexagon, Layers3, LineChart, LockKeyhole,
  LogOut, Map as MapIcon, Menu, MonitorCog, MoreHorizontal, Network, Pause,
  Play, RefreshCw, Search, Settings2, ShieldCheck, SlidersHorizontal, Sparkles,
  Target, Timer, Users, Video, Wifi, WifiOff, X, Zap
} from 'lucide-react';
import './index.css';

type Mode = 'admin' | 'officer';
type CameraStatus = 'Online' | 'Degraded' | 'Offline';
type Severity = 'Critical' | 'High' | 'Medium' | 'Low';

const cameras = [
  { id: 'PN-LAD-04', sector: 'Ladakh', site: 'Khardung La approach', status: 'Online' as CameraStatus, health: 98, heartbeat: '12 sec ago', detections: 14, coord: [21, 21], accent: 'amber' },
  { id: 'PN-LAD-07', sector: 'Ladakh', site: 'Chushul relay', status: 'Online' as CameraStatus, health: 96, heartbeat: '18 sec ago', detections: 8, coord: [25, 28], accent: 'sage' },
  { id: 'PN-RAJ-02', sector: 'Rajasthan', site: 'Longewala west', status: 'Online' as CameraStatus, health: 100, heartbeat: '06 sec ago', detections: 22, coord: [15, 45], accent: 'sage' },
  { id: 'PN-RAJ-09', sector: 'Rajasthan', site: 'Ghotaru post', status: 'Degraded' as CameraStatus, health: 71, heartbeat: '2 min ago', detections: 3, coord: [23, 48], accent: 'terra' },
  { id: 'PN-PUN-01', sector: 'Punjab', site: 'Ferozepur north', status: 'Online' as CameraStatus, health: 99, heartbeat: '09 sec ago', detections: 17, coord: [30, 34], accent: 'sage' },
  { id: 'PN-GUJ-06', sector: 'Gujarat', site: 'Rann east sector', status: 'Offline' as CameraStatus, health: 0, heartbeat: '43 min ago', detections: 0, coord: [24, 59], accent: 'terra' },
  { id: 'PN-WBE-03', sector: 'West Bengal', site: 'Siliguri corridor', status: 'Online' as CameraStatus, health: 97, heartbeat: '15 sec ago', detections: 11, coord: [59, 52], accent: 'sage' },
  { id: 'PN-SIK-02', sector: 'Sikkim', site: 'Nathu La ridge', status: 'Online' as CameraStatus, health: 94, heartbeat: '24 sec ago', detections: 9, coord: [65, 40], accent: 'amber' },
  { id: 'PN-ARP-05', sector: 'Arunachal Pradesh', site: 'Tawang north', status: 'Online' as CameraStatus, health: 91, heartbeat: '31 sec ago', detections: 6, coord: [78, 31], accent: 'sage' },
  { id: 'PN-ARP-08', sector: 'Arunachal Pradesh', site: 'Walong pass', status: 'Degraded' as CameraStatus, health: 64, heartbeat: '4 min ago', detections: 2, coord: [84, 38], accent: 'terra' },
  { id: 'PN-ASM-04', sector: 'Assam', site: 'Dhubri riverine', status: 'Online' as CameraStatus, health: 98, heartbeat: '08 sec ago', detections: 12, coord: [73, 60], accent: 'sage' },
  { id: 'PN-ASM-07', sector: 'Assam', site: 'Karimganj east', status: 'Online' as CameraStatus, health: 95, heartbeat: '20 sec ago', detections: 7, coord: [80, 64], accent: 'sage' },
];

const incidents = [
  { id: 'INC-2417', time: '18 Jun · 14:32', sector: 'Ladakh', title: 'Unidentified movement near relay corridor', severity: 'Critical' as Severity, camera: 'PN-LAD-04', state: 'Under review', evidence: 'Verified', kind: 'PERSON · VEHICLE' },
  { id: 'INC-2416', time: '18 Jun · 13:48', sector: 'Rajasthan', title: 'Vehicle pattern outside geofence', severity: 'High' as Severity, camera: 'PN-RAJ-02', state: 'Acknowledged', evidence: 'Verified', kind: 'VEHICLE' },
  { id: 'INC-2415', time: '18 Jun · 12:06', sector: 'Arunachal Pradesh', title: 'Optical obstruction on ridge camera', severity: 'Medium' as Severity, camera: 'PN-ARP-08', state: 'Investigating', evidence: 'Pending', kind: 'ENVIRONMENT' },
  { id: 'INC-2414', time: '18 Jun · 10:19', sector: 'Gujarat', title: 'Camera heartbeat missed threshold', severity: 'Low' as Severity, camera: 'PN-GUJ-06', state: 'Open', evidence: 'Not applicable', kind: 'SYSTEM' },
  { id: 'INC-2413', time: '17 Jun · 21:44', sector: 'West Bengal', title: 'Loitering event at service road', severity: 'High' as Severity, camera: 'PN-WBE-03', state: 'Closed', evidence: 'Verified', kind: 'PERSON' },
];

const detections = [
  ['14:32:19', 'PERSON', '94.8%', 'PN-LAD-04', 'Critical'],
  ['14:31:58', 'VEHICLE', '88.2%', 'PN-LAD-04', 'High'],
  ['13:48:06', 'VEHICLE', '91.6%', 'PN-RAJ-02', 'High'],
  ['12:06:42', 'OBSTRUCTION', '76.4%', 'PN-ARP-08', 'Medium'],
  ['11:53:21', 'PERSON', '82.1%', 'PN-PUN-01', 'Medium'],
];

const navGroups: Array<{ label: string; items: Array<[string, string, typeof Activity]> }> = [
  { label: 'Command', items: [['/admin', 'Overview', Gauge], ['/admin/map', 'Operations map', MapIcon], ['/admin/cameras', 'Cameras', Camera], ['/admin/incidents', 'Incidents', Flag]] },
  { label: 'Assurance', items: [['/admin/evidence', 'Evidence vault', FileCheck2], ['/admin/analytics', 'Analytics', LineChart], ['/admin/health', 'System health', Activity]] },
  { label: 'Control', items: [['/admin/access', 'Access & audit', Users]] },
];

const officerNav = [
  ['/officer', 'My sector', Target],
  ['/officer/watch', 'Camera watch', Video],
  ['/officer/alerts', 'Alert queue', Bell],
  ['/officer/map', 'Sector map', MapIcon],
  ['/officer/evidence', 'Capture evidence', FileCheck2],
] as const;

function cn(...classes: Array<string | false | undefined>) { return classes.filter(Boolean).join(' '); }

function StatusDot({ status = 'Online' }: { status?: string }) {
  const color = status === 'Online' || status === 'Verified' ? 'bg-[#71885b]' : status === 'Degraded' || status === 'Pending' ? 'bg-[#b27a3d]' : 'bg-[#a34e42]';
  return <span className={cn('inline-block h-2 w-2 rounded-full', color)} />;
}

function Wordmark({ light = false }: { light?: boolean }) {
  return <div className={cn('flex items-center gap-3', light ? 'text-[#efe8d5]' : 'text-[#344128]')}>
    <span className={cn('grid h-9 w-9 place-items-center border', light ? 'border-[#a5ad7c] bg-[#5c6a42]' : 'border-[#718052] bg-[#e6e1c7]')}>
      <Hexagon size={19} strokeWidth={1.7} />
    </span>
    <span><span className="block font-mono text-[10px] tracking-[.22em] opacity-70">INDIA COMMAND NETWORK</span><span className="block text-[17px] font-extrabold tracking-[.15em]">PRAHARI NETRA</span></span>
  </div>;
}

function DemoPill() { return <span className="demo-pill"><CircleDot size={10} /> DEMO MODE · LOCAL SIMULATION</span>; }

function IndiaMap({ compact = false, sector = 'All sectors', onCamera }: { compact?: boolean; sector?: string; onCamera?: (id: string) => void }) {
  const activeCameras = sector === 'All sectors' ? cameras : cameras.filter((camera) => camera.sector === sector);
  return <div className={cn('map-frame relative overflow-hidden', compact ? 'min-h-[330px]' : 'min-h-[580px]')}>
    <div className="absolute inset-0 map-grid opacity-70" />
    <div className="absolute left-4 top-4 z-10 flex items-center gap-3"><span className="eyebrow">LIVE OPERATIONS MAP</span><span className="font-mono text-[10px] text-[#b8bea4]">BASE LAYER · STATIC / OFFLINE</span></div>
    <div className="absolute right-4 top-4 z-10 flex gap-2"><button className="map-control" data-testid="button-map-layers" onClick={() => window.alert('Layer controls are fixed for the local simulation.') }><Layers3 size={14} /> Layers</button><button className="map-control" data-testid="button-map-refresh" onClick={() => window.alert('Static node state refreshed locally.') }><RefreshCw size={14} /> Sync</button></div>
    <svg viewBox="0 0 100 100" className="absolute inset-[6%] h-[88%] w-[88%]" role="img" aria-label="Stylized static map of India">
      <defs><filter id="map-shadow"><feDropShadow dx="0" dy="1" stdDeviation="1.4" floodColor="#0b1710" floodOpacity=".35" /></filter></defs>
      <path d="M39 5 L49 8 L53 15 L64 18 L69 26 L79 29 L84 39 L78 43 L81 50 L74 55 L77 62 L69 67 L64 78 L57 76 L53 87 L49 96 L44 87 L38 81 L37 72 L29 66 L24 58 L16 55 L18 47 L12 41 L19 34 L24 25 L29 20 L31 11 Z" className="india-land" filter="url(#map-shadow)" />
      <path d="M25 27 Q42 35 58 27 M18 48 Q38 49 69 45 M28 65 Q49 59 75 61 M39 13 Q42 43 44 84 M61 20 Q57 43 65 72" className="map-contour" />
      <path d="M18 49 Q29 44 43 45 M58 25 Q66 28 74 38 M50 72 Q59 64 68 65" className="map-route" />
      {activeCameras.map((camera) => <g key={camera.id} className="cursor-pointer" onClick={() => onCamera?.(camera.id)}>
        <circle cx={camera.coord[0]} cy={camera.coord[1]} r="2.8" className={cn('node-halo', camera.status === 'Offline' ? 'offline' : camera.status === 'Degraded' ? 'degraded' : '')} />
        <circle cx={camera.coord[0]} cy={camera.coord[1]} r="0.9" className="node-core" />
      </g>)}
      <text x="10" y="94" className="map-label">ARABIAN SEA</text><text x="71" y="90" className="map-label">BAY OF BENGAL</text>
    </svg>
    <div className="absolute bottom-4 left-4 z-10 flex flex-wrap gap-4 bg-[#1b2b21]/85 px-3 py-2 text-[10px] text-[#d9ddc6]"><span className="flex items-center gap-2"><StatusDot /> Online 9</span><span className="flex items-center gap-2"><StatusDot status="Degraded" /> Degraded 2</span><span className="flex items-center gap-2"><StatusDot status="Offline" /> Offline 1</span></div>
    {!compact && <div className="absolute bottom-4 right-4 z-10 border border-[#667452] bg-[#263526]/90 px-3 py-2 font-mono text-[10px] text-[#d9ddc6]">12 NODES · 08 SECTORS · T+00:00:14</div>}
  </div>;
}

function Button({ children, kind = 'secondary', onClick, testId, disabled = false, type = 'button' }: { children: ReactNode; kind?: 'primary' | 'secondary' | 'danger' | 'quiet'; onClick?: () => void; testId: string; disabled?: boolean; type?: 'button' | 'submit' | 'reset' }) {
  return <button type={type} data-testid={testId} onClick={onClick ?? (() => window.alert('Local demo action completed in this browser.'))} disabled={disabled} className={cn('action-button', `action-${kind}`, disabled && 'cursor-not-allowed opacity-50')}>{children}</button>;
}

function PageHeader({ eyebrow, title, detail, action }: { eyebrow: string; title: string; detail?: string; action?: React.ReactNode }) {
  return <div className="mb-7 flex flex-wrap items-end justify-between gap-4"><div><div className="eyebrow mb-2">{eyebrow}</div><h1 className="page-title">{title}</h1>{detail && <p className="mt-2 max-w-2xl text-sm text-muted-foreground">{detail}</p>}</div>{action}</div>;
}

function Metric({ label, value, detail, tone = 'default', icon: Icon }: { label: string; value: string; detail: string; tone?: 'default' | 'alert' | 'good'; icon: typeof Activity }) {
  return <div className="metric-panel"><div className="flex items-start justify-between"><span className="eyebrow">{label}</span><Icon size={16} className={tone === 'alert' ? 'text-[#a34e42]' : tone === 'good' ? 'text-[#71885b]' : 'text-[#a47840]'} /></div><div className="mt-3 text-3xl font-semibold tracking-tight">{value}</div><div className="mt-1 text-xs text-muted-foreground">{detail}</div></div>;
}

function AppShell({ mode, children }: { mode: Mode; children: ReactNode }) {
  const [location, setLocation] = useLocation();
  const [mobileOpen, setMobileOpen] = useState(false);
  const isAdmin = mode === 'admin';
  const nav: Array<{ href: string; label: string; Icon: typeof Activity; group: string }> = isAdmin ? navGroups.flatMap((group) => group.items.map(([href, label, Icon]) => ({ href, label, Icon, group: group.label }))) : officerNav.map(([href, label, Icon]) => ({ href, label, Icon, group: 'Field operations' }));
  return <div className={cn('min-h-[100dvh] bg-background', isAdmin ? 'shell-admin' : 'shell-officer')}>
    <aside className={cn('fixed inset-y-0 left-0 z-40 w-[248px] border-r border-sidebar-border bg-sidebar text-sidebar-foreground transition-transform duration-300 md:translate-x-0', mobileOpen ? 'translate-x-0' : '-translate-x-full')}>
      <div className="flex h-full flex-col">
        <div className="border-b border-sidebar-border px-5 py-5"><Wordmark light /></div>
        <div className="mx-4 mt-5 border border-[#65724f] bg-[#2a3929] px-3 py-3"><div className="flex items-center gap-2 text-[10px] font-bold tracking-[.12em] text-[#d0d4b8]"><StatusDot /> {isAdmin ? 'ADMIN COMMAND' : 'GROUND OFFICER'}</div><div className="mt-2 font-mono text-[10px] text-[#aeb79a]">{isAdmin ? 'NATIONAL OPERATIONS DESK' : 'ASSIGNED · LADAKH / NORTH'}</div></div>
        <nav className="flex-1 overflow-y-auto px-3 py-5">
          <div className="mb-3 px-3 font-mono text-[9px] tracking-[.18em] text-[#9ba58a]">{isAdmin ? 'COMMAND CONSOLE' : 'FIELD CONSOLE'}</div>
          {nav.map(({ href, label, Icon, group }) => <div key={href}>{isAdmin && nav.findIndex((n) => n.group === group && n.href === href) === nav.findIndex((n) => n.group === group) && <div className="mb-1 mt-4 px-3 font-mono text-[9px] tracking-[.18em] text-[#738063]">{group}</div>}<Link href={href} onClick={() => setMobileOpen(false)} data-testid={`link-nav-${label.toLowerCase().replace(/\s+/g, '-')}`} className={cn('nav-link', location === href && 'nav-link-active')}><Icon size={16} strokeWidth={1.7} /><span>{label}</span>{label === 'Alert queue' && <span className="ml-auto rounded-sm bg-[#a34e42] px-1.5 py-0.5 font-mono text-[9px] text-[#f3ead7]">03</span>}</Link></div>)}
        </nav>
        <div className="border-t border-sidebar-border p-4"><div className="mb-4 flex items-center gap-3"><span className="avatar">AS</span><div className="min-w-0"><div className="truncate text-xs font-semibold">{isAdmin ? 'A. Srinivasan' : 'R. Dorje'}</div><div className="font-mono text-[9px] text-[#9ba58a]">{isAdmin ? 'COMMAND ADMIN' : 'FIELD OFFICER · G2'}</div></div></div><Link href="/role-select" data-testid="link-switch-role" className="flex items-center gap-2 text-[11px] text-[#b9c39f] hover:text-[#e4e4c9]"><LogOut size={14} /> Switch workspace</Link></div>
      </div>
    </aside>
    {mobileOpen && <button aria-label="Close navigation" data-testid="button-close-navigation" className="fixed inset-0 z-30 bg-[#172118]/45 md:hidden" onClick={() => setMobileOpen(false)} />}
    <main className="md:pl-[248px]"><header className="sticky top-0 z-20 flex min-h-[68px] items-center justify-between border-b border-border bg-background/95 px-4 backdrop-blur md:px-8"><button className="mr-3 md:hidden" data-testid="button-open-navigation" onClick={() => setMobileOpen(true)}><Menu size={20} /></button><div className="flex items-center gap-3"><span className="hidden font-mono text-[10px] text-muted-foreground sm:block">SECURE LOCAL INSTANCE</span><DemoPill /></div><div className="flex items-center gap-3"><span className="hidden text-right sm:block"><span className="block text-xs font-semibold">{isAdmin ? 'National Command' : 'Ladakh · North sector'}</span><span className="font-mono text-[9px] text-muted-foreground">SESSION PN-LOCAL-7F2A</span></span><span className="avatar avatar-light">{isAdmin ? 'AS' : 'RD'}</span></div></header><div className="mx-auto max-w-[1500px] px-4 py-7 md:px-8 lg:px-10">{children}</div></main>
  </div>;
}

function Home() {
  const [, setLocation] = useLocation();
  return <div className="entry-screen min-h-[100dvh] overflow-hidden bg-[#253523] text-[#efe8d5]"><div className="entry-grain" /><div className="absolute inset-0 opacity-30"><div className="entry-grid h-full w-full" /></div><svg className="entry-map" viewBox="0 0 700 800" aria-hidden="true"><path d="M301 34l66 22 22 51 78 24 33 54 74 25 35 74-41 47 20 68-62 46 9 77-75 47-22 107-48-16-43 68-39-96-60-44-6-97-79-34-36-81 24-61-55-61 54-69 16-80 54-43 13-61z" /></svg><div className="relative z-10 flex min-h-[100dvh] flex-col px-6 py-7 md:px-12"><header className="flex items-start justify-between"><Wordmark light /><DemoPill /></header><div className="my-auto grid max-w-6xl gap-12 py-20 lg:grid-cols-[1.2fr_.8fr] lg:items-center"><div className="max-w-3xl"><div className="eyebrow mb-6 text-[#b9c790]">TRUSTED BORDER INTELLIGENCE · 01 / ENTRY</div><h1 className="entry-title">See the line.<br /><em>Know the signal.</em></h1><p className="mt-7 max-w-xl text-base leading-7 text-[#b9c6ab]">A disciplined command network for authorized security operations teams. Geospatial awareness, simulated detection, and evidence provenance — held in one accountable surface.</p><div className="mt-9 flex flex-wrap gap-3"><Button kind="primary" testId="button-enter-demo" onClick={() => setLocation('/login')}>Enter secure demo <ArrowRight size={16} /></Button><button className="border border-[#697957] px-4 py-2.5 text-xs text-[#c4cdb2] hover:bg-[#344631]" data-testid="button-read-brief" onClick={() => document.getElementById('entry-brief')?.scrollIntoView({ behavior: 'smooth' })}>Read system brief</button></div></div><div id="entry-brief" className="border-l border-[#73815d] pl-6 lg:ml-auto lg:max-w-sm"><div className="mb-5 font-mono text-[10px] tracking-[.2em] text-[#a6b28a]">SYSTEM CONDITION / LOCAL</div>{[['12', 'SIMULATED CAMERA NODES'], ['08', 'BORDER SECTORS'], ['SHA-256', 'EVIDENCE CHAIN']].map(([a, b]) => <div key={b} className="mb-5 flex items-end justify-between border-b border-[#42523d] pb-4"><span className="font-mono text-2xl text-[#e7e1c7]">{a}</span><span className="text-right text-[10px] tracking-[.12em] text-[#a6b28a]">{b}</span></div>)}<div className="flex items-center gap-2 text-xs text-[#c7cfb5]"><span className="signal-pulse" /> All local systems nominal</div></div></div><footer className="flex flex-wrap items-end justify-between gap-5 border-t border-[#4d5e47] pt-5 text-[10px] text-[#93a082]"><span>DEMO ENVIRONMENT · NO LIVE CCTV · NO GOVERNMENT DATA</span><span className="font-mono">BUILD 0.8.14 / INDIA REGION / UTC +05:30</span></footer></div></div>;
}

function Login() {
  const [, setLocation] = useLocation();
  const [notice, setNotice] = useState('');
  return <div className="auth-layout"><div className="auth-art"><Wordmark light /><div className="auth-art-copy"><div className="eyebrow text-[#b9c790]">AUTHORIZED ACCESS ONLY</div><h1>One surface.<br /><em>Clear accountability.</em></h1><p>Use the local demonstration environment to explore role-separated command and field workspaces.</p></div><div className="font-mono text-[10px] text-[#9da98b]">PN-LOCAL · AIR-GAPPED SIMULATION</div></div><div className="auth-panel"><div className="mb-12 flex items-center justify-between"><span className="eyebrow">SECURE SIGN-IN</span><Link href="/" data-testid="link-back-home" className="text-xs text-muted-foreground hover:text-foreground"><ArrowLeft size={14} className="mr-1 inline" /> Back</Link></div><div className="max-w-md"><h1 className="text-3xl font-semibold tracking-tight">Enter the demo instance</h1><p className="mt-3 text-sm leading-6 text-muted-foreground">This simulation is deterministic and runs entirely in your browser. No credentials are transmitted.</p><form className="mt-8 space-y-4" onSubmit={(event) => { event.preventDefault(); setNotice('Local identity accepted. Choose an operational workspace.'); setTimeout(() => setLocation('/role-select'), 400); }}><label className="field-label">Operator identifier<input data-testid="input-operator-id" className="field-input" autoComplete="username" defaultValue="operator.asrinivasan" /></label><label className="field-label">Access phrase<input data-testid="input-access-phrase" className="field-input" type="password" autoComplete="current-password" defaultValue="prahari-demo" /></label><label className="flex items-start gap-3 text-xs text-muted-foreground"><input data-testid="input-demo-acknowledgement" type="checkbox" className="mt-0.5 accent-[#71885b]" defaultChecked /> I understand this is fictional demo data with no live surveillance connection.</label>{notice && <div className="border border-[#b27a3d]/40 bg-[#b27a3d]/10 px-3 py-2 text-xs text-[#8d5f2e]">{notice}</div>}<Button kind="primary" type="submit" testId="button-authenticate">Authenticate locally <ArrowRight size={16} /></Button></form><div className="mt-8 flex items-center gap-2 border-t border-border pt-5 text-[10px] text-muted-foreground"><LockKeyhole size={13} /> Browser-only session · evidence actions are simulated</div></div></div></div>;
}

function RoleSelect() {
  const [, setLocation] = useLocation();
  const [selected, setSelected] = useState<Mode>('admin');
  return <div className="min-h-[100dvh] bg-[#e7e3cf] px-5 py-7 text-[#344128] md:px-12"><div className="mx-auto flex max-w-6xl items-center justify-between"><Wordmark /><Link href="/" data-testid="link-role-exit" className="text-xs text-[#66734e]">Exit demo</Link></div><div className="mx-auto max-w-5xl py-20"><div className="eyebrow">SESSION HANDOFF · 02 / ROLE</div><h1 className="mt-4 max-w-2xl text-4xl font-semibold tracking-tight md:text-6xl">Choose your operational view.</h1><p className="mt-5 max-w-xl text-sm leading-6 text-[#687250]">Roles are intentionally separated. The command view sees the network. The ground view sees the assignment.</p><div className="mt-12 grid gap-4 md:grid-cols-2"><RoleCard mode="admin" selected={selected === 'admin'} onClick={() => setSelected('admin')} onEnter={() => setLocation('/admin')} /><RoleCard mode="officer" selected={selected === 'officer'} onClick={() => setSelected('officer')} onEnter={() => setLocation('/officer')} /></div><div className="mt-8 flex items-center justify-between border-t border-[#c9c4a9] pt-5 text-[10px] text-[#78805f]"><span>ROLE CHANGES ARE RECORDED IN THE DEMO AUDIT LOG</span><span className="font-mono">LOCAL SESSION PN-LOCAL-7F2A</span></div></div></div>;
}

function RoleCard({ mode, selected, onClick, onEnter }: { mode: Mode; selected: boolean; onClick: () => void; onEnter: () => void }) {
  const admin = mode === 'admin';
  return <div className={cn('role-card', selected && 'role-card-selected')} onClick={onClick} data-testid={`card-role-${mode}`}><div className="flex items-start justify-between"><span className={cn('role-icon', admin ? 'role-icon-admin' : 'role-icon-officer')}>{admin ? <MonitorCog size={25} /> : <Crosshair size={25} />}</span><span className={cn('h-4 w-4 rounded-full border-2', selected ? 'border-[#71885b] bg-[#71885b]' : 'border-[#a5a88d]')} /></div><div className="mt-8"><div className="eyebrow">{admin ? 'NATIONAL OPERATIONS' : 'ASSIGNED SECTOR'}</div><h2 className="mt-2 text-2xl font-semibold">{admin ? 'Admin Command' : 'Ground Officer'}</h2><p className="mt-3 min-h-[48px] text-sm leading-6 text-[#687250]">{admin ? 'Observe all sectors, review incidents, inspect system health, and validate the chain of evidence.' : 'Respond to assigned alerts, watch sector cameras, capture field evidence, and escalate with context.'}</p></div><button className="mt-7 flex items-center gap-2 text-xs font-semibold text-[#4f653c] hover:text-[#a47840]" data-testid={`button-enter-role-${mode}`} onClick={(event) => { event.stopPropagation(); onEnter(); }}>Enter workspace <ArrowRight size={14} /></button></div>;
}

function AdminOverview() {
  const [, setLocation] = useLocation();
  return <><PageHeader eyebrow="NATIONAL OPERATIONS · 18 JUN 2025 / 14:36 IST" title="Command overview" detail="A single operational picture across the simulated border network." action={<Button kind="primary" testId="button-open-command-map" onClick={() => setLocation('/admin/map')}><MapIcon size={15} /> Open operations map</Button>} /><div className="grid gap-3 sm:grid-cols-2 xl:grid-cols-4"><Metric label="Active incidents" value="04" detail="02 requiring command review" tone="alert" icon={AlertTriangle} /><Metric label="Network cameras" value="11 / 12" detail="91.7% currently reporting" tone="good" icon={Camera} /><Metric label="AI detections · 24h" value="126" detail="+18 from prior period" icon={Sparkles} /><Metric label="Evidence integrity" value="99.2%" detail="124 of 125 verified" tone="good" icon={ShieldCheck} /></div><div className="mt-6 grid gap-6 xl:grid-cols-[1.5fr_.85fr]"><section className="panel overflow-hidden"><div className="flex items-center justify-between border-b border-border px-5 py-4"><div><div className="eyebrow">OPERATIONS PICTURE</div><h2 className="mt-1 text-base font-semibold">Border network status</h2></div><Link href="/admin/map" data-testid="link-view-full-map" className="text-xs font-semibold text-[#71885b]">View full map <ArrowRight size={13} className="ml-1 inline" /></Link></div><IndiaMap compact onCamera={(id) => setLocation(`/admin/cameras/${id}`)} /></section><section className="panel"><div className="border-b border-border px-5 py-4"><div className="eyebrow">COMMAND ATTENTION</div><h2 className="mt-1 text-base font-semibold">Priority queue</h2></div><div className="divide-y divide-border">{incidents.slice(0, 4).map((incident) => <Link href={`/admin/incidents?focus=${incident.id}`} key={incident.id} data-testid={`row-priority-${incident.id}`} className="block px-5 py-4 hover:bg-[#e8e5d3]"><div className="flex items-start justify-between gap-3"><span className={cn('severity', `severity-${incident.severity.toLowerCase()}`)}>{incident.severity}</span><span className="font-mono text-[10px] text-muted-foreground">{incident.time.split(' · ')[1]}</span></div><div className="mt-2 text-sm font-semibold">{incident.title}</div><div className="mt-2 flex items-center justify-between text-[10px] text-muted-foreground"><span>{incident.sector} · {incident.camera}</span><span>{incident.state}</span></div></Link>)}</div></section></div><div className="mt-6 grid gap-6 lg:grid-cols-[1fr_1fr]"><RecentDetections /><SystemReadiness /></div></>;
}

function RecentDetections() {
  return <section className="panel"><div className="flex items-center justify-between border-b border-border px-5 py-4"><div><div className="eyebrow">AI ACTIVITY</div><h2 className="mt-1 text-base font-semibold">Recent detections</h2></div><Link href="/admin/analytics" data-testid="link-detections-analytics" className="text-xs text-[#71885b]">Analytics <ArrowRight size={13} className="ml-1 inline" /></Link></div><div className="overflow-x-auto"><table className="data-table"><thead><tr><th>Time</th><th>Class</th><th>Confidence</th><th>Node</th></tr></thead><tbody>{detections.map(([time, type, confidence, camera]) => <tr key={time}><td className="font-mono text-[10px]">{time}</td><td><span className="class-chip">{type}</span></td><td>{confidence}</td><td className="font-mono text-[10px] text-muted-foreground">{camera}</td></tr>)}</tbody></table></div></section>;
}

function SystemReadiness() {
  return <section className="panel"><div className="border-b border-border px-5 py-4"><div className="eyebrow">SYSTEM READINESS</div><h2 className="mt-1 text-base font-semibold">Operational health</h2></div><div className="space-y-5 p-5">{[['Camera mesh', '98.4%', 'good'], ['AI inference queue', '184 ms', 'good'], ['Evidence storage', '68.2%', 'good'], ['Gujarat uplink', 'Offline', 'alert']].map(([label, value, tone]) => <div key={label}><div className="mb-2 flex justify-between text-xs"><span>{label}</span><span className={tone === 'alert' ? 'text-[#a34e42]' : 'text-[#71885b]'}>{value}</span></div><div className="h-1.5 bg-[#d7d5c1]"><div className={cn('h-full', tone === 'alert' ? 'w-[22%] bg-[#a34e42]' : 'w-[82%] bg-[#71885b]')} /></div></div>)}<Link href="/admin/health" data-testid="link-open-health" className="mt-1 block border-t border-border pt-4 text-xs font-semibold text-[#71885b]">Open health console <ArrowRight size={13} className="ml-1 inline" /></Link></div></section>;
}

function AdminMap() {
  const [, setLocation] = useLocation();
  const [sector, setSector] = useState('All sectors');
  const [selected, setSelected] = useState('');
  const sectors = ['All sectors', ...Array.from(new Set(cameras.map((camera) => camera.sector)))];
  const camera = cameras.find((item) => item.id === selected);
  return <><PageHeader eyebrow="COMMAND CONSOLE · GEOSPATIAL" title="Operations map" detail="Static India treatment for the local simulation. No external map tiles or live location services." action={<div className="flex gap-2"><Button kind="secondary" testId="button-recenter-map" onClick={() => setSelected('')}><Target size={14} /> Recenter</Button><Button kind="primary" testId="button-map-export" onClick={() => window.alert('Map view exported to the local download queue.') }><Download size={14} /> Export view</Button></div>} /><div className="grid gap-5 xl:grid-cols-[1fr_300px]"><div className="panel p-2"><IndiaMap sector={sector} onCamera={setSelected} /></div><aside className="panel p-5"><div className="eyebrow">FILTER NETWORK</div><label className="field-label mt-5">Sector<select className="field-input" data-testid="select-map-sector" value={sector} onChange={(event) => setSector(event.target.value)}>{sectors.map((item) => <option key={item}>{item}</option>)}</select></label><div className="my-6 border-y border-border py-5"><div className="eyebrow mb-3">LEGEND</div><div className="space-y-3 text-xs"><div className="flex justify-between"><span className="flex items-center gap-2"><StatusDot /> Online</span><span>09</span></div><div className="flex justify-between"><span className="flex items-center gap-2"><StatusDot status="Degraded" /> Degraded</span><span>02</span></div><div className="flex justify-between"><span className="flex items-center gap-2"><StatusDot status="Offline" /> Offline</span><span>01</span></div></div></div>{camera ? <div className="border border-[#b4b596] bg-[#e8e5d3] p-4"><div className="flex items-center justify-between"><span className="class-chip">{camera.id}</span><StatusDot status={camera.status} /></div><h3 className="mt-3 text-sm font-semibold">{camera.site}</h3><p className="mt-1 text-xs text-muted-foreground">{camera.sector}</p><button className="mt-4 text-xs font-semibold text-[#71885b]" data-testid="button-open-selected-camera" onClick={() => setLocation(`/admin/cameras/${camera.id}`)}>Open camera <ArrowRight size={13} className="ml-1 inline" /></button></div> : <div className="empty-state"><MapIcon size={24} /><p>Select a node on the map to inspect its sector context.</p></div>}</aside></div></>;
}

function AdminCameras() {
  const [, setLocation] = useLocation();
  const [query, setQuery] = useState('');
  const [filter, setFilter] = useState('All');
  const filtered = cameras.filter((camera) => (filter === 'All' || camera.status === filter) && `${camera.id} ${camera.sector} ${camera.site}`.toLowerCase().includes(query.toLowerCase()));
  return <><PageHeader eyebrow="COMMAND CONSOLE · SENSOR NETWORK" title="Camera explorer" detail="Health, heartbeat, and simulated AI activity across all border nodes." action={<Button kind="primary" testId="button-camera-refresh" onClick={() => window.alert('Camera mesh refreshed from the local node registry.') }><RefreshCw size={14} /> Refresh mesh</Button>} /><div className="panel"><div className="flex flex-wrap items-center justify-between gap-3 border-b border-border p-4"><label className="relative block min-w-[240px] flex-1"><Search size={15} className="absolute left-3 top-2.5 text-muted-foreground" /><input data-testid="input-camera-search" className="field-input pl-9" placeholder="Search node, sector, or site" value={query} onChange={(event) => setQuery(event.target.value)} /></label><div className="flex gap-1">{['All', 'Online', 'Degraded', 'Offline'].map((item) => <button key={item} onClick={() => setFilter(item)} data-testid={`button-camera-filter-${item.toLowerCase()}`} className={cn('filter-button', filter === item && 'filter-button-active')}>{item}</button>)}</div></div><div className="grid gap-3 p-4 md:grid-cols-2 xl:grid-cols-3">{filtered.map((camera) => <button key={camera.id} data-testid={`card-camera-${camera.id}`} onClick={() => setLocation(`/admin/cameras/${camera.id}`)} className="camera-card text-left"><div className="camera-thumb"><Video size={23} /><span className={cn('camera-corner', camera.status === 'Offline' ? 'text-[#d88878]' : 'text-[#b7c889]')}>{camera.status === 'Offline' ? 'NO SIGNAL' : 'SIMULATED FEED'}</span><span className="absolute bottom-3 right-3 flex items-center gap-1 font-mono text-[9px] text-[#d7dfc2]"><span className="signal-pulse" /> LIVE</span></div><div className="p-4"><div className="flex justify-between gap-3"><div><div className="font-mono text-[10px] text-muted-foreground">{camera.id}</div><h3 className="mt-1 text-sm font-semibold">{camera.site}</h3></div><StatusDot status={camera.status} /></div><div className="mt-4 grid grid-cols-3 gap-2 border-t border-border pt-3 text-[10px]"><span><span className="block text-muted-foreground">SECTOR</span><span className="mt-1 block">{camera.sector}</span></span><span><span className="block text-muted-foreground">HEALTH</span><span className="mt-1 block">{camera.health || '—'}%</span></span><span><span className="block text-muted-foreground">AI / 24H</span><span className="mt-1 block">{camera.detections}</span></span></div></div></button>)}</div>{!filtered.length && <div className="empty-state m-5"><Search size={24} /><p>No camera nodes match that filter.</p><button onClick={() => { setQuery(''); setFilter('All'); }} data-testid="button-reset-camera-filter" className="text-xs font-semibold text-[#71885b]">Reset filters</button></div>}</div></>;
}

function CameraDetail() {
  const { id } = useParams<{ id: string }>();
  const camera = cameras.find((item) => item.id === id) ?? cameras[0];
  const [, setLocation] = useLocation();
  const [playing, setPlaying] = useState(true);
  const [note, setNote] = useState('');
  return <><PageHeader eyebrow={`CAMERA NODE · ${camera.id}`} title={camera.site} detail={`${camera.sector} sector · Last heartbeat ${camera.heartbeat}`} action={<div className="flex gap-2"><Button kind="secondary" testId="button-camera-back" onClick={() => setLocation('/admin/cameras')}><ArrowLeft size={14} /> All cameras</Button><Button kind="primary" testId="button-create-incident" onClick={() => setNote('Incident draft created from camera context.')}>Create incident <Flag size={14} /></Button></div>} />{note && <div className="mb-5 flex items-center justify-between border border-[#71885b]/30 bg-[#71885b]/10 px-4 py-3 text-xs text-[#526c43]">{note}<button onClick={() => setNote('')} data-testid="button-dismiss-camera-note"><X size={14} /></button></div>}<div className="grid gap-6 xl:grid-cols-[1.35fr_.65fr]"><div className="space-y-6"><section className="feed-panel"><div className="feed-top"><span className="flex items-center gap-2"><span className="signal-pulse" /> SIMULATED CCTV · {camera.id}</span><span className="font-mono">18 JUN 2025 · 14:36:22 IST</span></div><div className="feed-image"><div className="feed-scan" /><div className="ai-box ai-box-one"><span>PERSON 94.8%</span></div><div className="ai-box ai-box-two"><span>VEHICLE 88.2%</span></div><div className="feed-reticle"><Crosshair size={46} /></div><div className="absolute bottom-4 left-4 font-mono text-[10px] text-[#d5ddbf]">LAT 34.1154 N / LONG 77.5432 E</div></div><div className="flex items-center justify-between border-t border-[#4c6449] bg-[#18271e] px-4 py-3 text-[#b9c9a1]"><div className="flex items-center gap-3"><button onClick={() => setPlaying(!playing)} data-testid="button-toggle-feed" className="grid h-8 w-8 place-items-center border border-[#607251]">{playing ? <Pause size={14} /> : <Play size={14} />}</button><span className="font-mono text-[10px]">{playing ? 'STREAM SIMULATION ACTIVE' : 'STREAM PAUSED'}</span></div><span className="font-mono text-[10px]">1080P · 12 FPS · ENCRYPTED</span></div></section><section className="panel"><div className="border-b border-border px-5 py-4"><div className="eyebrow">EVENT TIMELINE</div><h2 className="mt-1 text-base font-semibold">Detection and operator activity</h2></div><div className="timeline p-5">{[['14:32:19', 'AI detection', 'Person class above confidence threshold', 'AI ENGINE'], ['14:31:58', 'AI detection', 'Vehicle class entered geofence corridor', 'AI ENGINE'], ['14:30:44', 'Heartbeat', 'Node reported nominal health at 98%', 'SYSTEM'], ['13:48:10', 'Operator note', 'A. Srinivasan opened camera context', 'COMMAND']].map(([time, title, text, actor]) => <div className="timeline-item" key={time}><span className="timeline-dot" /><div className="flex flex-wrap justify-between gap-2"><span className="text-xs font-semibold">{title}</span><span className="font-mono text-[10px] text-muted-foreground">{time} · {actor}</span></div><p className="mt-1 text-xs text-muted-foreground">{text}</p></div>)}</div></section></div><aside className="space-y-6"><section className="panel"><div className="border-b border-border px-5 py-4"><div className="eyebrow">NODE METADATA</div><h2 className="mt-1 text-base font-semibold">Trust and connectivity</h2></div><div className="divide-y divide-border">{[['Status', camera.status], ['Device certificate', 'Valid · 41 days'], ['mTLS channel', 'Established'], ['Firmware', 'PN-CAM 3.14.2'], ['Last heartbeat', camera.heartbeat], ['Storage buffer', camera.status === 'Offline' ? 'Staged locally' : '22% used']].map(([key, value]) => <div key={key} className="flex justify-between gap-3 px-5 py-3 text-xs"><span className="text-muted-foreground">{key}</span><span className="text-right font-medium">{value}</span></div>)}</div></section><section className="panel p-5"><div className="eyebrow">AI ACTIVITY</div><div className="mt-4 flex items-end justify-between"><span className="text-3xl font-semibold">{camera.detections}</span><span className="text-xs text-muted-foreground">events / last 24h</span></div><div className="mini-bars mt-5">{[25, 42, 31, 55, 42, 68, 48, 72, 63, 86, 54, 78].map((height, index) => <span key={index} style={{ height: `${height}%` }} />)}</div><Link href="/admin/evidence" data-testid="link-camera-evidence" className="mt-5 block text-xs font-semibold text-[#71885b]">Inspect linked evidence <ArrowRight size={13} className="ml-1 inline" /></Link></section></aside></div></>;
}

function AdminIncidents() {
  const [query, setQuery] = useState('');
  const [severity, setSeverity] = useState('All');
  const filtered = incidents.filter((item) => (severity === 'All' || item.severity === severity) && `${item.id} ${item.title} ${item.sector}`.toLowerCase().includes(query.toLowerCase()));
  return <><PageHeader eyebrow="COMMAND CONSOLE · INCIDENT MANAGEMENT" title="Incident timeline" detail="Every event retains its operational context and evidence state." action={<Button kind="secondary" testId="button-export-incidents" onClick={() => window.alert('Incident log exported to the local download queue.') }><Download size={14} /> Export log</Button>} /><div className="panel"><div className="flex flex-wrap gap-3 border-b border-border p-4"><label className="relative min-w-[240px] flex-1"><Search size={15} className="absolute left-3 top-2.5 text-muted-foreground" /><input className="field-input pl-9" data-testid="input-incident-search" placeholder="Search incident, camera, or sector" value={query} onChange={(event) => setQuery(event.target.value)} /></label><select className="field-input w-auto" data-testid="select-incident-severity" value={severity} onChange={(event) => setSeverity(event.target.value)}>{['All', 'Critical', 'High', 'Medium', 'Low'].map((value) => <option key={value}>{value}</option>)}</select></div><div className="divide-y divide-border">{filtered.map((item) => <div key={item.id} className="incident-row" data-testid={`row-incident-${item.id}`}><div className="flex min-w-0 flex-1 gap-4"><div className="hidden w-14 shrink-0 pt-1 text-right font-mono text-[10px] text-muted-foreground sm:block">{item.time.split(' · ')[1]}</div><div className="incident-line" /><div className="min-w-0"><div className="flex flex-wrap items-center gap-2"><span className="font-mono text-[10px] text-muted-foreground">{item.id}</span><span className={cn('severity', `severity-${item.severity.toLowerCase()}`)}>{item.severity}</span></div><h3 className="mt-2 text-sm font-semibold">{item.title}</h3><p className="mt-1 text-xs text-muted-foreground">{item.sector} · {item.camera} · {item.kind}</p></div></div><div className="hidden items-end gap-3 text-right md:flex md:flex-col"><span className="status-label">{item.state}</span><span className="flex items-center gap-1 text-[10px] text-[#71885b]"><ShieldCheck size={12} /> {item.evidence}</span></div><button className="self-center text-muted-foreground hover:text-foreground" data-testid={`button-incident-menu-${item.id}`} onClick={() => window.alert(`Local action menu for ${item.id}`)}><MoreHorizontal size={17} /></button></div>)}</div>{!filtered.length && <div className="empty-state m-5"><Search size={23} /><p>No incidents match the current view.</p></div>}</div></>;
}

function AdminEvidence() {
  const [tampered, setTampered] = useState(false);
  const [verified, setVerified] = useState(false);
  return <><PageHeader eyebrow="ASSURANCE · PROVENANCE" title="Evidence vault" detail="Cryptographic provenance records for simulated evidence objects. The compromised record is intentionally controlled and clearly marked." action={<Button kind="secondary" testId="button-export-evidence"><Download size={14} /> Export manifest</Button>} /><div className="mb-5 flex items-start gap-3 border border-[#b27a3d]/35 bg-[#b27a3d]/10 p-4 text-xs text-[#76562e]"><AlertTriangle size={16} className="mt-0.5 shrink-0" /><div><strong>Controlled tamper demonstration.</strong> One fictional object below can be marked tampered to show how the verification workflow surfaces a chain break. No files leave this browser.</div></div><div className="grid gap-6 xl:grid-cols-[1fr_360px]"><section className="panel"><div className="border-b border-border px-5 py-4"><div className="eyebrow">OBJECT MANIFEST · 05 RECORDS</div><h2 className="mt-1 text-base font-semibold">Evidence objects</h2></div><div className="divide-y divide-border">{[['EV-2417-A', 'PN-LAD-04', '18 Jun · 14:32:21', 'Verified'], ['EV-2416-B', 'PN-RAJ-02', '18 Jun · 13:48:09', 'Verified'], ['EV-2415-A', 'PN-ARP-08', '18 Jun · 12:06:44', 'Pending'], ['EV-2409-C', 'PN-GUJ-06', '17 Jun · 08:11:02', tampered ? 'Tampered' : 'Compromised demo']].map(([id, node, time, state]) => <div className="flex flex-wrap items-center gap-4 px-5 py-4" key={id}><span className={cn('grid h-8 w-8 place-items-center border', state === 'Tampered' || state === 'Compromised demo' ? 'border-[#a34e42]/40 text-[#a34e42]' : 'border-[#71885b]/40 text-[#71885b]')}><FileKey2 size={15} /></span><div className="min-w-[130px] flex-1"><div className="font-mono text-[10px]">{id}</div><div className="mt-1 text-xs text-muted-foreground">{node} · {time}</div></div><span className={cn('status-label', state === 'Verified' ? 'status-good' : state === 'Pending' ? 'status-warn' : 'status-bad')}>{state}</span><button className="text-xs font-semibold text-[#71885b]" data-testid={`button-verify-${id}`} onClick={() => setVerified(true)}>Verify <Check size={13} className="ml-1 inline" /></button></div>)}</div></section><section className="space-y-5"><div className="panel p-5"><div className="eyebrow">VERIFICATION RESULT</div><div className={cn('mt-4 flex items-center gap-3 border p-4', tampered ? 'border-[#a34e42]/50 bg-[#a34e42]/10' : 'border-[#71885b]/40 bg-[#71885b]/10')}><span className={cn('grid h-9 w-9 place-items-center rounded-full', tampered ? 'bg-[#a34e42] text-[#f8ead9]' : 'bg-[#71885b] text-[#eff2dc]')}>{tampered ? <X size={18} /> : <BadgeCheck size={18} />}</span><div><div className="text-sm font-semibold">{tampered ? 'Chain break detected' : 'Chain verified'}</div><div className="mt-1 text-xs text-muted-foreground">{verified ? 'EV-2417-A checked just now.' : tampered ? 'EV-2409-C does not match its signed digest.' : 'Last full verification · 14:35:08 IST'}</div></div></div><div className="mt-5 space-y-3 text-xs">{[['SHA-256 digest', tampered ? 'Mismatch' : 'Matched'], ['Operator signature', 'Ed25519 · valid'], ['Device certificate', 'PN-CAM-04 · valid'], ['mTLS session', 'Established'], ['RBAC custody', '2-of-2 recorded']].map(([label, value]) => <div className="flex items-center justify-between border-b border-border pb-2" key={label}><span className="text-muted-foreground">{label}</span><span className={tampered && label === 'SHA-256 digest' ? 'text-[#a34e42]' : 'text-[#526c43]'}>{value}</span></div>)}</div></div><div className="panel p-5"><div className="eyebrow">DEMO CONTROL</div><p className="mt-3 text-xs leading-5 text-muted-foreground">Toggle a fictional compromised object to demonstrate chain-of-custody alerting.</p><button className={cn('mt-4 w-full border px-3 py-2.5 text-xs font-semibold', tampered ? 'border-[#71885b] text-[#526c43]' : 'border-[#a34e42]/50 text-[#a34e42]')} data-testid="button-controlled-tamper" onClick={() => { setTampered(!tampered); setVerified(false); }}>{tampered ? 'Restore verified demo state' : 'Mark EV-2409-C as tampered'}</button></div></section></div></>;
}

function AdminAnalytics() {
  const [windowSize, setWindowSize] = useState('24 hours');
  return <><PageHeader eyebrow="ASSURANCE · PATTERN REVIEW" title="Operational analytics" detail="Trends are generated from the deterministic local event set." action={<select className="field-input w-auto" data-testid="select-analytics-window" value={windowSize} onChange={(event) => setWindowSize(event.target.value)}>{['24 hours', '7 days', '30 days'].map((value) => <option key={value}>{value}</option>)}</select>} /><div className="grid gap-3 sm:grid-cols-2 xl:grid-cols-4"><Metric label="Events reviewed" value="126" detail={`Window · ${windowSize}`} icon={Eye} /><Metric label="Review completion" value="83.6%" detail="105 of 126 triaged" tone="good" icon={CheckCircle2} /><Metric label="Median confidence" value="88.4%" detail="Across person / vehicle" icon={Sparkles} /><Metric label="Alert precision" value="76.1%" detail="Based on reviewed events" tone="good" icon={Target} /></div><div className="mt-6 grid gap-6 xl:grid-cols-[1.25fr_.75fr]"><section className="panel p-5"><div className="eyebrow">EVENT TREND · {windowSize.toUpperCase()}</div><div className="mt-2 flex items-end justify-between"><h2 className="text-base font-semibold">Detection volume</h2><span className="text-xs text-[#71885b]">+18.2%</span></div><div className="line-chart mt-7"><div className="chart-lines" />{[32, 44, 40, 58, 49, 73, 62, 82, 66, 91, 77, 84, 72, 94].map((height, i) => <span key={i} style={{ height: `${height}%` }}><i /></span>)}</div><div className="mt-3 flex justify-between font-mono text-[9px] text-muted-foreground"><span>00:00</span><span>06:00</span><span>12:00</span><span>18:00</span><span>NOW</span></div></section><section className="panel"><div className="border-b border-border px-5 py-4"><div className="eyebrow">SECTOR ACTIVITY</div><h2 className="mt-1 text-base font-semibold">Events by sector</h2></div><div className="space-y-4 p-5">{[['Ladakh', '38', 86], ['Rajasthan', '27', 66], ['Punjab', '18', 51], ['Arunachal Pradesh', '16', 44], ['Other sectors', '27', 67]].map(([label, value, width]) => <div key={label}><div className="mb-1 flex justify-between text-xs"><span>{label}</span><span className="font-mono text-[10px] text-muted-foreground">{value}</span></div><div className="h-2 bg-[#d7d5c1]"><div className="h-full bg-[#71885b]" style={{ width: `${width}%` }} /></div></div>)}</div></section></div><div className="mt-6 grid gap-6 md:grid-cols-2"><section className="panel p-5"><div className="eyebrow">UPTIME</div><h2 className="mt-1 text-base font-semibold">Camera availability</h2><div className="mt-6 grid grid-cols-6 gap-2">{cameras.map((camera) => <div key={camera.id} className="text-center"><div className={cn('uptime-block', camera.status === 'Online' ? 'bg-[#71885b]' : camera.status === 'Degraded' ? 'bg-[#b27a3d]' : 'bg-[#a34e42]')} /><div className="mt-2 truncate font-mono text-[8px] text-muted-foreground">{camera.id.replace('PN-', '')}</div></div>)}</div></section><section className="panel p-5"><div className="eyebrow">REVIEW QUEUE</div><h2 className="mt-1 text-base font-semibold">AI confidence bands</h2><div className="mt-6 space-y-4">{[['High confidence · 90–100%', '68%', 'w-[68%]'], ['Review band · 70–89%', '24%', 'w-[24%]'], ['Low confidence · below 70%', '08%', 'w-[8%]']].map(([label, value, width], i) => <div key={label}><div className="mb-1 flex justify-between text-xs"><span>{label}</span><span>{value}</span></div><div className="h-2 bg-[#d7d5c1]"><div className={cn('h-full', i === 0 ? 'bg-[#71885b]' : i === 1 ? 'bg-[#b27a3d]' : 'bg-[#a34e42]', width)} /></div></div>)}</div></section></div></>;
}

function AdminHealth() {
  const [refreshing, setRefreshing] = useState(false);
  const checks: Array<[string, string, string, 'good' | 'warn', typeof Activity]> = [['Camera mesh', 'Operational', '12 / 12 handshakes within window', 'good', Wifi], ['AI inference', 'Operational', '184 ms median · local edge queue', 'good', Sparkles], ['API relay', 'Operational', 'Local simulation bus · 0 dropped', 'good', Network], ['Evidence storage', 'Operational', '68.2% capacity · hash index healthy', 'good', Database], ['Gujarat uplink', 'Attention', 'Last heartbeat 43 minutes ago', 'warn', WifiOff]];
  return <><PageHeader eyebrow="CONTROL · HEALTH TELEMETRY" title="System health" detail="Infrastructure signals for this browser-local environment." action={<Button kind="secondary" testId="button-refresh-health" onClick={() => { setRefreshing(true); setTimeout(() => setRefreshing(false), 700); }}><RefreshCw size={14} className={refreshing ? 'animate-spin' : ''} /> {refreshing ? 'Checking…' : 'Run checks'}</Button>} /><div className="grid gap-4 md:grid-cols-2">{checks.map(([label, value, detail, tone, Icon]) => <div className="health-card" key={label}><div className={cn('health-icon', tone === 'warn' && 'health-icon-warn')}><Icon size={19} /></div><div className="min-w-0 flex-1"><div className="flex flex-wrap items-center justify-between gap-2"><h2 className="text-sm font-semibold">{label}</h2><span className={cn('status-label', tone === 'good' ? 'status-good' : 'status-warn')}><StatusDot status={tone === 'good' ? 'Online' : 'Degraded'} /> {value}</span></div><p className="mt-2 text-xs text-muted-foreground">{detail}</p></div></div>)}</div><div className="mt-6 grid gap-6 lg:grid-cols-2"><section className="panel p-5"><div className="eyebrow">LATENCY DISTRIBUTION</div><h2 className="mt-1 text-base font-semibold">AI processing by minute</h2><div className="mt-6 flex items-end gap-2">{[30, 44, 35, 58, 42, 38, 63, 48, 44, 52, 39, 46, 34, 41, 37, 45].map((height, index) => <div key={index} className="flex-1 bg-[#a47840]" style={{ height: `${height * 2.2}px` }} />)}</div><div className="mt-3 flex justify-between font-mono text-[9px] text-muted-foreground"><span>14:20</span><span>14:28</span><span>14:36</span></div></section><section className="panel p-5"><div className="eyebrow">SERVICE NOTES</div><h2 className="mt-1 text-base font-semibold">Recent health events</h2><div className="mt-5 space-y-4">{['14:35 · Full mesh heartbeat accepted', '14:21 · Gujarat node missed 3 heartbeats', '13:58 · Evidence index compacted', '12:40 · AI model checksum confirmed'].map((note) => <div className="flex gap-3 text-xs" key={note}><span className="mt-1 h-1.5 w-1.5 shrink-0 rounded-full bg-[#71885b]" /><span>{note}</span></div>)}</div></section></div></>;
}

function AdminAccess() {
  const [audit, setAudit] = useState(false);
  const users = [['A. Srinivasan', 'Command Admin', 'Active', '18 Jun · 14:31'], ['R. Dorje', 'Ground Officer · G2', 'Active', '18 Jun · 14:28'], ['N. Chatterjee', 'Evidence Reviewer', 'Active', '17 Jun · 20:14'], ['S. Mehta', 'System Auditor', 'Suspended', '04 Jun · 11:02']];
  return <><PageHeader eyebrow="CONTROL · ROLE SEPARATION" title="Access & audit" detail="Identity, permissions, and immutable local audit entries. This view is unavailable to ground officers." action={<Button kind="primary" testId="button-invite-user" onClick={() => setAudit(true)}><Users size={14} /> Simulate invite</Button>} />{audit && <div className="mb-5 flex items-center justify-between border border-[#71885b]/30 bg-[#71885b]/10 px-4 py-3 text-xs text-[#526c43]">Invite flow staged locally. No message was sent.<button data-testid="button-dismiss-invite" onClick={() => setAudit(false)}><X size={14} /></button></div>}<div className="grid gap-6 xl:grid-cols-[1.2fr_.8fr]"><section className="panel"><div className="border-b border-border px-5 py-4"><div className="eyebrow">IDENTITY DIRECTORY · 04</div><h2 className="mt-1 text-base font-semibold">Authorized operators</h2></div><div className="divide-y divide-border">{users.map(([name, role, status, seen], index) => <div className="flex items-center gap-3 px-5 py-4" key={name}><span className="avatar avatar-light">{name.split(' ').map((part) => part[0]).join('')}</span><div className="min-w-0 flex-1"><div className="text-sm font-semibold">{name}</div><div className="mt-1 text-[10px] text-muted-foreground">{role} · Last seen {seen}</div></div><span className={cn('status-label', status === 'Active' ? 'status-good' : 'status-bad')}>{status}</span><button className="text-muted-foreground" data-testid={`button-user-settings-${index}`} onClick={() => setAudit(true)}><Settings2 size={15} /></button></div>)}</div></section><section className="panel"><div className="border-b border-border px-5 py-4"><div className="eyebrow">PERMISSION MODEL</div><h2 className="mt-1 text-base font-semibold">Role boundaries</h2></div><div className="divide-y divide-border">{[['Command Admin', 'All sectors', '14 permissions'], ['Evidence Reviewer', 'All sectors', '08 permissions'], ['Ground Officer · G2', 'Ladakh / North', '06 permissions']].map(([role, scope, count]) => <div className="px-5 py-4" key={role}><div className="flex justify-between text-xs font-semibold"><span>{role}</span><span className="font-mono text-[10px] text-muted-foreground">{count}</span></div><div className="mt-2 text-[10px] text-muted-foreground">{scope}</div><div className="mt-3 flex flex-wrap gap-1.5"><span className="permission-chip">READ</span><span className="permission-chip">REVIEW</span>{role.includes('Admin') && <span className="permission-chip">MANAGE ACCESS</span>}</div></div>)}</div></section></div><section className="panel mt-6"><div className="border-b border-border px-5 py-4"><div className="eyebrow">AUDIT LOG · IMMUTABLE DEMO STREAM</div><h2 className="mt-1 text-base font-semibold">Recent access events</h2></div><div className="overflow-x-auto"><table className="data-table"><thead><tr><th>Timestamp</th><th>Actor</th><th>Action</th><th>Context</th><th>Trace</th></tr></thead><tbody>{[['14:31:08', 'A. Srinivasan', 'Opened incident INC-2417', 'Command', '7f2a…1d0c'], ['14:28:41', 'R. Dorje', 'Acknowledged alert ALT-034', 'Ladakh', '9a18…c441'], ['14:12:06', 'N. Chatterjee', 'Verified evidence EV-2416-B', 'Assurance', '4c90…ad12']].map((row) => <tr key={row[0]}>{row.map((cell) => <td key={cell}>{cell}</td>)}</tr>)}</tbody></table></div></section></>;
}

function OfficerHome() {
  const [, setLocation] = useLocation();
  return <><PageHeader eyebrow="FIELD CONSOLE · LADAKH / NORTH" title="Good afternoon, R." detail="Your assigned picture — focused, current, and ready for action." action={<Button kind="primary" testId="button-open-officer-watch" onClick={() => setLocation('/officer/watch')}><Video size={14} /> Open camera watch</Button>} /><div className="mb-6 flex items-center gap-3 border border-[#71885b]/30 bg-[#71885b]/10 px-4 py-3 text-xs text-[#526c43]"><ShieldCheck size={16} /> Assigned-sector access active · 2 cameras reporting · 1 alert requires acknowledgement</div><div className="grid gap-3 sm:grid-cols-3"><Metric label="Open alerts" value="03" detail="1 high priority" tone="alert" icon={Bell} /><Metric label="Sector cameras" value="02 / 02" detail="Both online" tone="good" icon={Camera} /><Metric label="Evidence staged" value="07" detail="Last capture 13:58 IST" icon={FileCheck2} /></div><div className="mt-6 grid gap-6 xl:grid-cols-[1.1fr_.9fr]"><section className="panel"><div className="border-b border-border px-5 py-4"><div className="eyebrow">ASSIGNED SECTOR</div><h2 className="mt-1 text-base font-semibold">Ladakh / North operational picture</h2></div><IndiaMap compact sector="Ladakh" onCamera={(id) => setLocation(`/officer/watch?camera=${id}`)} /></section><section className="panel"><div className="border-b border-border px-5 py-4"><div className="eyebrow">NEXT ACTIONS</div><h2 className="mt-1 text-base font-semibold">Field queue</h2></div><div className="divide-y divide-border">{[['Acknowledge', 'INC-2417 · Unidentified movement', 'Critical', '/officer/alerts'], ['Review feed', 'PN-LAD-04 · Person detection', '94.8%', '/officer/watch'], ['Capture evidence', 'Add context to open incident', 'Optional', '/officer/evidence']].map(([action, title, detail, href]) => <Link href={href} data-testid={`link-officer-action-${action.toLowerCase().replace(' ', '-')}`} className="flex items-center gap-4 px-5 py-4 hover:bg-[#e8e5d3]" key={title}><span className="grid h-8 w-8 place-items-center border border-[#b4b596] text-[#71885b]">{action === 'Acknowledge' ? <Bell size={14} /> : action === 'Review feed' ? <Eye size={14} /> : <FileCheck2 size={14} />}</span><div className="min-w-0 flex-1"><div className="text-xs font-semibold">{title}</div><div className="mt-1 text-[10px] text-muted-foreground">{action}</div></div><span className="font-mono text-[10px] text-[#a47840]">{detail}</span><ArrowRight size={14} className="text-muted-foreground" /></Link>)}</div></section></div></>;
}

function OfficerWatch() {
  const [selected, setSelected] = useState(cameras[0]);
  const [playing, setPlaying] = useState(true);
  const sectorCameras = cameras.filter((camera) => camera.sector === 'Ladakh');
  return <><PageHeader eyebrow="FIELD CONSOLE · LIVE SIMULATION" title="Camera watch" detail="Two cameras in your assigned Ladakh / North sector. Tap a node to change feed." action={<div className="flex items-center gap-2"><span className="status-label status-good"><span className="signal-pulse" /> Watch active</span><Button kind="secondary" testId="button-watch-refresh" onClick={() => window.alert('Assigned camera watch refreshed locally.') }><RefreshCw size={14} /> Refresh</Button></div>} /><div className="grid gap-5 xl:grid-cols-[1fr_310px]"><section className="feed-panel"><div className="feed-top"><span className="flex items-center gap-2"><span className="signal-pulse" /> {selected.id} · {selected.site}</span><span className="font-mono">14:36:22 IST</span></div><div className="feed-image"><div className="feed-scan" /><div className="ai-box ai-box-one"><span>PERSON 94.8%</span></div><div className="ai-box ai-box-two"><span>VEHICLE 88.2%</span></div><div className="absolute left-4 top-4 border border-[#c2d39a] bg-[#304b32]/75 px-2 py-1 font-mono text-[9px] text-[#deebc1]">AI OVERLAY · ACTIVE</div><div className="feed-reticle"><Crosshair size={46} /></div></div><div className="flex items-center justify-between border-t border-[#4c6449] bg-[#18271e] px-4 py-3 text-[#b9c9a1]"><button onClick={() => setPlaying(!playing)} data-testid="button-officer-toggle-feed" className="flex items-center gap-2 text-xs">{playing ? <Pause size={14} /> : <Play size={14} />}{playing ? 'Pause feed' : 'Resume feed'}</button><span className="font-mono text-[10px]">HEALTH {selected.health}% · {selected.heartbeat}</span></div></section><aside className="panel"><div className="border-b border-border px-5 py-4"><div className="eyebrow">ASSIGNED NODES</div><h2 className="mt-1 text-base font-semibold">Select a feed</h2></div><div className="divide-y divide-border">{sectorCameras.map((camera) => <button key={camera.id} onClick={() => setSelected(camera)} data-testid={`button-select-watch-${camera.id}`} className={cn('w-full px-5 py-4 text-left hover:bg-[#e8e5d3]', selected.id === camera.id && 'bg-[#dedfc7]')}><div className="flex items-center justify-between"><span className="font-mono text-[10px]">{camera.id}</span><StatusDot status={camera.status} /></div><div className="mt-2 text-xs font-semibold">{camera.site}</div><div className="mt-2 flex justify-between text-[10px] text-muted-foreground"><span>AI events {camera.detections}</span><span>{camera.health}% health</span></div></button>)}</div><div className="border-t border-border p-5"><div className="eyebrow">LAST DETECTIONS</div><div className="mt-4 space-y-3">{detections.slice(0, 3).map(([time, type, conf]) => <div key={time} className="flex justify-between text-[10px]"><span className="flex items-center gap-2"><Sparkles size={12} className="text-[#a47840]" /> {type}</span><span className="font-mono text-muted-foreground">{time} · {conf}</span></div>)}</div></div></aside></div></>;
}

function OfficerAlerts() {
  const [items, setItems] = useState(incidents.slice(0, 4));
  const [message, setMessage] = useState('');
  const act = (id: string, action: string) => { setItems((current) => current.map((item) => item.id === id ? { ...item, state: action } : item)); setMessage(`${id} marked ${action.toLowerCase()} in the local field log.`); };
  return <><PageHeader eyebrow="FIELD CONSOLE · RESPONSE QUEUE" title="Alert queue" detail="Triage alerts with clear handoffs. Actions update only this local simulation." action={<Button kind="secondary" testId="button-alert-filter"><SlidersHorizontal size={14} /> Filter queue</Button>} />{message && <div className="mb-5 flex justify-between border border-[#71885b]/30 bg-[#71885b]/10 px-4 py-3 text-xs text-[#526c43]">{message}<button onClick={() => setMessage('')} data-testid="button-dismiss-alert-message"><X size={14} /></button></div>}<div className="space-y-3">{items.map((item) => <article className="panel p-5" key={item.id} data-testid={`card-officer-alert-${item.id}`}><div className="flex flex-wrap items-start justify-between gap-4"><div className="flex gap-3"><span className={cn('mt-1 grid h-9 w-9 place-items-center border', item.severity === 'Critical' ? 'border-[#a34e42]/50 text-[#a34e42]' : 'border-[#b27a3d]/50 text-[#a47840]')}><AlertTriangle size={17} /></span><div><div className="flex flex-wrap items-center gap-2"><span className="font-mono text-[10px] text-muted-foreground">{item.id}</span><span className={cn('severity', `severity-${item.severity.toLowerCase()}`)}>{item.severity}</span><span className="status-label">{item.state}</span></div><h2 className="mt-2 text-sm font-semibold">{item.title}</h2><p className="mt-2 text-xs text-muted-foreground">{item.sector} · {item.camera} · {item.time}</p></div></div><Link href={`/officer/incidents/${item.id}`} data-testid={`link-investigate-${item.id}`} className="text-xs font-semibold text-[#71885b]">Investigate <ArrowRight size={13} className="ml-1 inline" /></Link></div><div className="mt-5 flex flex-wrap gap-2 border-t border-border pt-4"><Button kind="secondary" testId={`button-acknowledge-${item.id}`} onClick={() => act(item.id, 'Acknowledged')}><Check size={14} /> Acknowledge</Button><Button kind="secondary" testId={`button-investigate-${item.id}`} onClick={() => act(item.id, 'Investigating')}><Eye size={14} /> Investigate</Button><Button kind="danger" testId={`button-escalate-${item.id}`} onClick={() => act(item.id, 'Escalated')}><Zap size={14} /> Escalate</Button></div></article>)}</div></>;
}

function OfficerIncident() {
  const { id } = useParams<{ id: string }>();
  const item = incidents.find((incident) => incident.id === id) ?? incidents[0];
  const [, setLocation] = useLocation();
  const [state, setState] = useState(item.state);
  return <><PageHeader eyebrow={`FIELD INCIDENT · ${item.id}`} title={item.title} detail={`${item.sector} · ${item.camera} · Detected ${item.time}`} action={<Button kind="secondary" testId="button-back-alerts" onClick={() => setLocation('/officer/alerts')}><ArrowLeft size={14} /> Alert queue</Button>} /><div className="grid gap-6 xl:grid-cols-[1fr_350px]"><div className="space-y-6"><section className="panel p-5"><div className="flex flex-wrap items-center gap-2"><span className={cn('severity', `severity-${item.severity.toLowerCase()}`)}>{item.severity}</span><span className="status-label">{state}</span><span className="status-label status-good"><ShieldCheck size={12} /> Evidence verified</span></div><div className="mt-6 grid gap-4 sm:grid-cols-3"><div><div className="eyebrow">DETECTION CLASS</div><div className="mt-2 text-sm font-semibold">{item.kind}</div></div><div><div className="eyebrow">CONFIDENCE</div><div className="mt-2 text-sm font-semibold">94.8%</div></div><div><div className="eyebrow">GEOFENCE</div><div className="mt-2 text-sm font-semibold">0.42 km inside</div></div></div></section><section className="panel"><div className="border-b border-border px-5 py-4"><div className="eyebrow">EVENT CHRONOLOGY</div><h2 className="mt-1 text-base font-semibold">What happened</h2></div><div className="timeline p-5">{[['14:32:19', 'Detection generated', 'AI engine identified PERSON + VEHICLE classes above review threshold.'], ['14:32:28', 'Alert routed', 'Assigned to R. Dorje · G2 field response.'], ['14:34:05', 'Evidence sealed', 'Frame set EV-2417-A signed and added to the local chain.'], ['Now', 'Field action', 'Current status is controlled by the officer workspace.']].map(([time, title, text]) => <div className="timeline-item" key={time}><span className="timeline-dot" /><span className="font-mono text-[10px] text-muted-foreground">{time}</span><h3 className="mt-1 text-xs font-semibold">{title}</h3><p className="mt-1 text-xs text-muted-foreground">{text}</p></div>)}</div></section></div><aside className="space-y-6"><div className="panel p-5"><div className="eyebrow">LOCATION CONTEXT</div><IndiaMap compact sector="Ladakh" /><div className="mt-4 grid grid-cols-2 gap-3 text-xs"><div><span className="block text-muted-foreground">COORDINATES</span><span className="mt-1 block font-mono text-[10px]">34.1154 N<br />77.5432 E</span></div><div><span className="block text-muted-foreground">CAMERA</span><span className="mt-1 block font-mono text-[10px]">{item.camera}</span></div></div></div><div className="panel p-5"><div className="eyebrow">FIELD ACTION</div><div className="mt-4 grid gap-2"><Button kind="primary" testId="button-incident-acknowledge" onClick={() => setState('Acknowledged')}><Check size={14} /> Acknowledge</Button><Button kind="danger" testId="button-incident-escalate" onClick={() => setState('Escalated')}><Zap size={14} /> Escalate to command</Button><Link href="/officer/evidence" data-testid="link-capture-incident-evidence" className="action-button action-secondary justify-center"><FileCheck2 size={14} /> Capture evidence</Link></div></div></aside></div></>;
}

function OfficerEvidence() {
  const [captured, setCaptured] = useState(false);
  const [verified, setVerified] = useState(false);
  return <><PageHeader eyebrow="FIELD CONSOLE · PROVENANCE" title="Capture evidence" detail="Create a sealed evidence object from the selected incident context." action={<span className="status-label status-good"><LockKeyhole size={12} /> Local chain available</span>} /><div className="grid gap-6 xl:grid-cols-[1fr_380px]"><section className="panel p-5"><div className="eyebrow">CAPTURE WORKFLOW</div><h2 className="mt-2 text-lg font-semibold">Seal a field observation</h2><div className="mt-6 space-y-5"><div className="workflow-step"><span className="workflow-number">01</span><div><h3 className="text-sm font-semibold">Select source</h3><select className="field-input mt-3" data-testid="select-evidence-source" defaultValue="PN-LAD-04 · 14:32:19"><option>PN-LAD-04 · 14:32:19</option><option>PN-LAD-07 · 14:31:04</option></select></div></div><div className="workflow-step"><span className="workflow-number">02</span><div className="flex-1"><h3 className="text-sm font-semibold">Add field context</h3><textarea className="field-input mt-3 min-h-[95px]" data-testid="textarea-evidence-context" placeholder="Describe what you observed, without inference." defaultValue="Movement observed along the relay corridor. Frame set retained for command review." /></div></div><div className="workflow-step"><span className="workflow-number">03</span><div><h3 className="text-sm font-semibold">Seal and sign</h3><p className="mt-2 text-xs text-muted-foreground">A SHA-256 digest, operator signature, device certificate, and custody record will be simulated locally.</p></div></div></div><Button kind="primary" testId="button-seal-evidence" onClick={() => { setCaptured(true); setVerified(false); }}><Fingerprint size={15} /> Seal evidence object</Button></section><aside className="panel p-5"><div className="eyebrow">VERIFICATION OUTPUT</div>{captured ? <div className="mt-5"><div className="border border-[#71885b]/40 bg-[#71885b]/10 p-4"><div className="flex items-center gap-2 text-sm font-semibold text-[#526c43]"><CheckCircle2 size={17} /> Evidence sealed locally</div><div className="mt-3 font-mono text-[10px] leading-5 text-muted-foreground">EV-2418-A<br />SHA-256 · 3d7c…9a12<br />ED25519 · SIGNED<br />CHAIN INDEX · 0126</div></div><Button kind="secondary" testId="button-verify-captured-evidence" onClick={() => setVerified(true)}>{verified ? <Check size={14} /> : <ShieldCheck size={14} />} {verified ? 'Integrity verified' : 'Verify integrity'}</Button></div> : <div className="empty-state min-h-[270px]"><FileCheck2 size={28} /><p>Complete the capture steps to generate a verifiable object.</p></div>}</aside></div></>;
}

function OfficerMap() {
  const [, setLocation] = useLocation();
  return <><PageHeader eyebrow="FIELD CONSOLE · ASSIGNED GEOGRAPHY" title="Sector map" detail="Your view is scoped to Ladakh / North. National context and access controls stay with command." action={<Button kind="secondary" testId="button-officer-map-recenter"><Target size={14} /> Recenter assignment</Button>} /><div className="panel p-2"><IndiaMap sector="Ladakh" onCamera={(id) => setLocation(`/officer/watch?camera=${id}`)} /></div><div className="mt-5 grid gap-3 sm:grid-cols-3"><Metric label="Assigned cameras" value="02" detail="Both reporting" tone="good" icon={Camera} /><Metric label="Open geofences" value="04" detail="2 active this shift" icon={Target} /><Metric label="Response distance" value="0.42 km" detail="Nearest detection" icon={Timer} /></div></>;
}

function Router() {
  return <Switch><Route path="/" component={Home} /><Route path="/login" component={Login} /><Route path="/role-select" component={RoleSelect} /><Route path="/admin"><AppShell mode="admin"><AdminOverview /></AppShell></Route><Route path="/admin/map"><AppShell mode="admin"><AdminMap /></AppShell></Route><Route path="/admin/cameras/:id"><AppShell mode="admin"><CameraDetail /></AppShell></Route><Route path="/admin/cameras"><AppShell mode="admin"><AdminCameras /></AppShell></Route><Route path="/admin/incidents"><AppShell mode="admin"><AdminIncidents /></AppShell></Route><Route path="/admin/evidence"><AppShell mode="admin"><AdminEvidence /></AppShell></Route><Route path="/admin/analytics"><AppShell mode="admin"><AdminAnalytics /></AppShell></Route><Route path="/admin/health"><AppShell mode="admin"><AdminHealth /></AppShell></Route><Route path="/admin/access"><AppShell mode="admin"><AdminAccess /></AppShell></Route><Route path="/officer"><AppShell mode="officer"><OfficerHome /></AppShell></Route><Route path="/officer/watch"><AppShell mode="officer"><OfficerWatch /></AppShell></Route><Route path="/officer/alerts"><AppShell mode="officer"><OfficerAlerts /></AppShell></Route><Route path="/officer/incidents/:id"><AppShell mode="officer"><OfficerIncident /></AppShell></Route><Route path="/officer/evidence"><AppShell mode="officer"><OfficerEvidence /></AppShell></Route><Route path="/officer/map"><AppShell mode="officer"><OfficerMap /></AppShell></Route><Route><NotFound /></Route></Switch>;
}

function NotFound() {
  const [, setLocation] = useLocation();
  return <div className="grid min-h-[100dvh] place-items-center bg-[#e7e3cf] p-6"><div className="text-center"><div className="eyebrow">SIGNAL NOT FOUND</div><h1 className="mt-3 text-4xl font-semibold">This route is outside the demo perimeter.</h1><button className="action-button action-primary mt-6" data-testid="button-return-entry" onClick={() => setLocation('/')}>Return to entry <ArrowLeft size={15} /></button></div></div>;
}

export default function App() {
  return <Router />;
}